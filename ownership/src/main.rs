fn main() {
    // stack & heap
    // references
    // there can only be one owner at a time
    // rust is extremely scope dependent { foobar }
    
    let x  = String::from("Hello this is a basic string"); 
    x_reference = &x;

}
